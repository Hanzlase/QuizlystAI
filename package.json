{
  "name": "quizlyst-ai-auth",
  "version": "1.0.0",
  "description": "Authentication system for Quizlyst AI",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'Building assets' && cp -r public public_build",
    "vercel-build": "npm run build",
    "reset-db": "node reset-db.js"
  },
  "engines": {
    "node": ">=18.x"
  },
  "dependencies": {
    "axios": "^1.10.0",
    "cheerio": "^1.1.2",
    "cors": "^2.8.5",
    "dotenv": "^16.0.3",
    "express": "^4.18.2",
    "mammoth": "^1.9.1",
    "multer": "^2.0.2",
    "pdf-parse": "^1.1.1",
    "youtube-transcript": "^1.2.1",
    "@vercel/node": "^3.0.0"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}